Data directory for this project.

- `data/raw/`: raw inputs (not tracked if large)
- `data/processed/`: cleaned datasets used for modeling (generated by scripts)

Large datasets and generated files are intentionally excluded from version control.
